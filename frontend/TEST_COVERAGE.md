# Frontend behavioral audit coverage

This is a **provisional execution-coverage baseline for five audited workflows**, not a completed behavioral baseline or comprehensive frontend coverage. The focused suites contain 123 cases: 109 intended behaviors pass and 14 are explicit expected-failure reproductions of seven unresolved product defects. Vitest displays 123 passes because `it.fails` inverts those known failures; it does not mean that their acceptance criteria are satisfied. No production behavior or permission policy was changed.

## Reproduce the focused evidence

Use the repository-supported Node 24 and npm 11, then run from `frontend`:

```bash
npm ci
npm run test:audit
npm run test:coverage:audit
```

`test:audit` runs only the eight files listed below. `test:coverage:audit` runs the identical files and enforces the committed scoped thresholds. Neither command runs the full frontend, backend, or browser suite. Reports are generated under the ignored `frontend/coverage/audit/` directory: terminal text, inspectable `index.html`, and machine-readable `coverage-summary.json`.

Vitest and `@vitest/coverage-v8` resolve to 3.2.7 in the lockfile. The separate [audit configuration](vitest.audit.config.js) explicitly includes every audited source, including unimported files, with `coverage.all: true`; dependencies, test helpers, mocks, and generated output are excluded. Keep this collection scope unchanged when comparing measurements.

## Executable scenario matrix

Paths below are relative to `frontend`; the first column refers to the local frontend test coverage specification's numbered user stories. “Pass” means the stated controlled-HTTP behavior passed, not that all related product functionality is complete.

| Stories  | Suite and public boundary                                                                                                      | Verified result and limits                                                                                                                                                                                                                                                                     |
| -------- | ------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1, 47–48 | This matrix, reproducible commands, and defect inventory                                                                       | Exact audit scope and passing versus unresolved behavior are distinguished.                                                                                                                                                                                                                    |
| 2        | `src/router/__tests__/roles.test.js`: actual public router, setup complete, unauthenticated user                               | All 17 protected destination variants reach Login without prohibited route work.                                                                                                                                                                                                               |
| 3–5      | Same router: Admin/Investigator/Analyst × admin/client destinations                                                            | Six allowed/denied cases pass.                                                                                                                                                                                                                                                                 |
| 6–7      | Same router: three roles × legacy/scoped Hunt dashboard/execution                                                              | Twelve cases pass; Analysts start no Hunt request, permitted users reach canonical destinations.                                                                                                                                                                                               |
| 8        | `src/components/__tests__/TaskDetailWorkflow.test.js` and `TaskTableWorkflow.test.js`                                          | Admin/full edit, Case lead/full edit, assigned Investigator/quick edit, unrelated users/no edit, creation/deletion role controls and assignment server denial pass. Assigned Analyst edit denial remains a known failure.                                                                      |
| 9        | `src/services/__tests__/session.integration.test.js`: real Axios request interceptors                                          | Current/rotated/default-type/absent-token headers pass.                                                                                                                                                                                                                                        |
| 10–11    | Same suite: mounted App, real router listener, auth/active Case stores, HTTP 401                                               | Tokens, user and Case authorization clear; one visible snackbar and Login destination pass.                                                                                                                                                                                                    |
| 12       | Same session suite: concurrent 401s, then independent expiration after suppression window                                      | Coherent visible notification/navigation and a later independently handled session pass.                                                                                                                                                                                                       |
| 13       | Same session suite: HTTP 403 and 500                                                                                           | Distinct error responses preserve session, authorization and route.                                                                                                                                                                                                                            |
| 14–16    | `src/views/__tests__/Register.test.js`: mounted Register, actual Vuetify form, invite service and route                        | Assigned roles, missing/invalid/expired/used links, pending validation, detail/fallback failures and return to Login pass.                                                                                                                                                                     |
| 17       | Same registration suite: eight invalid-input variants                                                                          | Button validation passes. All eight native public-submit no-request assertions remain expected failures. Separately, real Chromium rejects short-username and mismatched-password Enter submissions; the native-submit defect must not be described as failure of those actual keyboard paths. |
| 18–21    | Same registration suite: exact invite payload, pending submission, rejection/retry, success and delayed navigation             | Valid button/public-submit payloads pass; repeated button submission is blocked; retained input and retry pass; success stays unauthenticated and returns to Login at 2000ms.                                                                                                                  |
| 22       | Disposable-backend browser registration journey                                                                                | Browser verification is recorded separately below.                                                                                                                                                                                                                                             |
| 23       | Task detail: real full form → store → service → HTTP                                                                           | Exact scalar edit payload, pending state, duplicate-submit block and acknowledged list/detail synchronization pass. Rejected full custom-field editing remains defective.                                                                                                                      |
| 24–25    | Task detail: assigned Investigator quick edit and inline custom-field edit                                                     | Actual dialog submission saves status/custom fields and preserves unrelated custom fields.                                                                                                                                                                                                     |
| 26–27    | Task table/detail: real status and assignment dialogs                                                                          | Deferred status acknowledgement has no premature success; assign/unassign use actual user selection and null query contract; 403/500 errors and retries pass.                                                                                                                                  |
| 28       | Task table: real ConfirmationDialog                                                                                            | Cancel sends no request. Acknowledged delete removes only the confirmed Task and clears its detail.                                                                                                                                                                                            |
| 29–30    | Task detail/table: rejected mutations and subsequent public retries                                                            | Scalar full/quick/status/assignment state preservation and retry pass; deletion preserves state and retries but lacks visible feedback. Full-form custom-field rejection corrupts acknowledged detail.                                                                                         |
| 31       | Task detail and bulk store: explicitly deferred responses across Case changes                                                  | Original target remains fixed; late responses leave current Case list/detail data intact.                                                                                                                                                                                                      |
| 32–33    | `src/stores/__tests__/taskBulkWorkflow.test.js`: real store/service/HTTP                                                       | Full/partial/empty bulk assignment/status results, exact selected IDs/payloads, null unassignment, unrelated records, rejection and retry pass. Dashboard bulk controls remain unfinished, outside this boundary.                                                                              |
| 34       | `src/components/__tests__/EvidenceTemplateManagementCard.integration.test.js`: actual management card/recursive editor/service | Loading, failure/Retry and an existing template with no folders pass. Entirely empty template collection feedback remains defective.                                                                                                                                                           |
| 35–37    | Same management suite: recursive root/descendant controls and save                                                             | Root/grandchild edits, nested/root additions/deletions, sibling/other-template preservation, exact hierarchy payload and deferred acknowledgement pass.                                                                                                                                        |
| 38       | Same management suite: failed save and retry                                                                                   | Save Changes resubmits the retained draft successfully. The advertised Retry button discards it: known failure.                                                                                                                                                                                |
| 39       | `src/views/__tests__/EvidenceTemplates.integration.test.js`: real CaseDashboard → EvidenceList → selection dialog and preview  | Loading, required selection, nested descriptions, selected-empty and collection-empty states, and Cancel without write pass.                                                                                                                                                                   |
| 40–42    | Same application suite: Admin/Case 42 and Investigator/Case 77, actual parent refresh wiring                                   | Exact Case/template request, duplicate pending block, no early refresh, acknowledged refresh GET and visible tree, rejection and retained-selection retry pass. Analyst application control is absent.                                                                                         |
| 43–44    | All eight suites: actual controls, public submit/click/Escape, isolated lifecycle                                              | Fresh stores/storage/router state, restored transport and listeners/timers, deterministic promises. Template selection Escape restores focus. Recursive editor actions lack accessible names: known failure. Inline Task Source editing is located by its visible field row.                   |
| 45–46    | Explicit source collection and four-metric total/per-source thresholds                                                         | Provisional measured execution floors prevent silent regression in this scope; unresolved behavior is not considered covered.                                                                                                                                                                  |

The eight configuration-listed suites are the router and session suites, registration suite, two mounted Task suites, one bulk Task suite, management-template suite, and application-template suite. The audit command excludes browser and existing unrelated regression suites; additional affected-boundary checks are recorded below.

## Measurement and threshold policy

Final values are recorded in [coverage.audit-baseline.json](coverage.audit-baseline.json), with exact source paths and four metrics (lines, statements, functions, branches). The configuration applies both the audited total and each source's measured percentages as minimums. Per-source floors prevent one module's improvement from masking a decrease in another. No arbitrary repository-wide percentage is imposed, and thresholds are not automatically lowered or updated.

Measured with Node 24.20.0 / Vitest 3.2.7 / V8 3.2.7 over the 28-source, eight-suite scope (workflow test snapshot `30a990d`):

| Metric     | Covered / total | Measured | Audited-total minimum |
| ---------- | --------------- | -------- | --------------------- |
| Lines      | 4135 / 5012     | 82.50%   | 82.50%                |
| Statements | 4135 / 5012     | 82.50%   | 82.50%                |
| Functions  | 155 / 344       | 45.05%   | 45.05%                |
| Branches   | 571 / 731       | 78.11%   | 78.11%                |

Every included file also has its own four measured minimums in the JSON baseline.

All 28 full production modules in `auditedSources` are included, including the unused `useEvidenceTemplates.js` composable at zero coverage. It remains visible deliberately. Shared source files contain functionality beyond the audited paths: general Evidence CRUD/download/selection handlers in EvidenceList and its service; unrelated CaseDashboard sections/actions; admin invite management; template creation and broad Task filtering; setup/account fallback handling; and router lazy page import callbacks. These are not added to the audit solely to raise a number. Inspect the HTML report for the current uncovered lines and branches.

Vue SFC/static-render code and lazy imports influence these metrics. Execution counts include code reached by confirmed failing reproductions, so the percentages cannot certify the corresponding behavior. Zero floors on an unexecuted file document the current gap; they do not promise its behavior works. A production fix should convert the linked `it.fails` to an ordinary passing test and review any resulting threshold increase.

To intentionally update a floor, run the unchanged scoped command, inspect its JSON/HTML results and scenario results, then review changes to the exact matching entries in `coverage.audit-baseline.json`. If source/test scope changes, update the configuration and this report together and measure that same scope before enforcing new floors. Preserve the distinction between execution coverage and unresolved acceptance.

## Unresolved product defects

The local tracker is ignored by Git, so this table preserves enough detail for a reviewer who does not have `.scratch`. Each of the first seven issues has an executable expected-failure reproduction in the named tracked suite. Before marking them `it.fails`, implementers ran the same intended assertions normally and verified failure at the stated behavior, rather than a harness/setup error. An unexpected pass now fails the suite and signals that the marker needs removal. The browser suite adds one separately marked failure after real fixture setup; its observed fallback-dashboard request is summarized below.

| Local issue under `.scratch/frontend-test-coverage-defects/issues/` | Expected / observed behavior and reproduction                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `02-registration-validation.md`                                     | Invalid details/password mismatch must send no invite-registration request through native submit. Eight invalid variants send HTTP despite the disabled button. Register suite's eight native-submit no-request assertions reproduce this. Actual Chromium invalid-username/mismatched-password Enter checks pass, so those real keyboard paths are not claimed defective.                                                                                                              |
| `03-tasks-custom-field-draft.md`                                    | Rejected full edit must retain acknowledged custom fields. TaskForm aliases `props.task.custom_fields`, so typing changes `currentTask` before acknowledgement; rejected PUT leaves the unsaved source. TaskDetailWorkflow asserts original fields after rejection.                                                                                                                                                                                                                     |
| `03-tasks-delete-feedback.md`                                       | Failed confirmed deletion must show useful feedback. CaseTasks/TaskTable keep data but only log the error and never render `taskStore.error`. TaskTableWorkflow asserts visible server error after DELETE rejection.                                                                                                                                                                                                                                                                    |
| `03-tasks-analyst-controls.md`                                      | Assigned Analysts remain read-only under backend `CaseAccess.writable`. TaskDetail offers Quick Edit based on assignment despite that known role restriction. TaskDetailWorkflow asserts the control is absent.                                                                                                                                                                                                                                                                         |
| `04-templates-save-retry-discards-draft.md`                         | Failed-save Retry must retain edited hierarchy. It reloads saved templates and replaces the draft; management suite verifies the subsequent payload should retain the edited name. Save Changes is an alternate working retry, not a fix for this control.                                                                                                                                                                                                                              |
| `04-templates-missing-empty-state.md`                               | An entirely empty template collection must have understandable feedback. Management displays no such empty state; the named empty-state assertion fails.                                                                                                                                                                                                                                                                                                                                |
| `04-templates-unnamed-folder-actions.md`                            | Recursive folder action buttons need accessible names. The actual editor's icon buttons have none; the management suite preserves an accessible-name assertion. Hierarchy tests use selectors scoped to visible folder rows and are not claimed as accessible-name coverage.                                                                                                                                                                                                            |
| `05-browser-analyst-clients.md`                                     | An allowed Analyst Cases dashboard should not request the backend-forbidden client list. Real Chromium sees GET `/api/clients/` from `useDashboard.loadData` after dashboard login or restricted-route fallback; `ClientService.get_clients` rejects Analysts. The independent browser reproduction asserts no client request only after the real fixture and visible Case row succeed, with `test.fail` immediately before that assertion. Route destinations remain correctly denied. |
| `05-browser-client-without-email.md` (ancillary)                    | Fresh-backend POST `/api/clients/` with a unique name and omitted optional email should return 201 but returns 400. Source inspection identifies duplicate `None` email matching the seeded Personal client. Browser Case fixtures now supply distinct valid emails; this unrelated product defect has no new expected-failure unit test and is not claimed covered or fixed.                                                                                                           |

The Task dashboard's unimplemented bulk handlers remain separately tracked by the existing `frontend-task-bulk-actions` local issue. These tests exercise the public bulk store/service contract only; they do not create browser controls or invent per-item error details absent from the API.

## Verification record

The exact scoped measurement command is `npm run test:coverage:audit` (eight files). It reports 123 runner passes, comprising 109 passing intended cases and 14 expected failures. New-suite subtotals: role/session 40 passing; registration 23 passing + 8 expected failures; Tasks 33 passing + 3 expected failures; templates 13 passing + 3 expected failures.

Affected-boundary verification was also performed during implementation:

- Role/session plus existing router/auth/HTTP/App files: seven files, 75 passing tests. Existing failure fixtures emit their expected logging; a subsequent final new-suite run was clean.
- Tasks plus existing table/form/quick-edit/store files: seven files, 39 passing tests + 3 expected failures. A final table-only fixture refinement run passed (9 intended cases + 1 expected failure).
- Templates plus existing EvidenceList/CaseDashboard files: four files, 36 passing tests + 3 expected failures.
- All new/changed test and configuration JavaScript received focused ESLint and Prettier checks. No full frontend/backend suite was run.

### Real-backend browser verification

Run from the repository root with Node 24 available:

```bash
E2E_SERVER_KINDS=gateway E2E_HOSTS=127.0.0.1 E2E_VIEWPORTS=desktop \
E2E_GATEWAY_PORT=18080 E2E_GATEWAY_HTTPS_PORT=18443 E2E_BACKEND_PORT=18000 \
./scripts/test-first-run-browser-journey.sh e2e/frontend-workflows.spec.js
```

Verified on the built gateway frontend, Chromium desktop, `127.0.0.1`, and a disposable PostgreSQL/backend stack. The runner reported four passes in 13.7s: **three real acceptance journeys and one explicit expected-failure reproduction**. The three successful journeys independently establish required data and verify:

1. Fresh invite role display; actual invalid/mismatched Enter sends no request; valid Enter redeems the invite; success remains unauthenticated; actual UI login authenticates the created user; the invite is now used.
2. Independent Analyst account/assigned Case; six admin/client/legacy and scoped Hunt URLs return to Cases, restricted pages do not render, no admin/Hunt work starts, and the assigned Case stays accessible. The fallback dashboard's forbidden client-list request remains in its own failing reproduction.
3. Independent Task; full edit through named fields and Enter; exact acknowledged PUT; API GET, reload, and list navigation retain edited title and description.

No browser HTTP responses were mocked. The browser expected failure concerns the Analyst fallback client request. An earlier run exposed optional-email client fixture setup failures; fixtures were corrected to use distinct valid emails and the product defect was preserved separately. Actual Chromium keyboard checks narrow the native-submit component failure as described above.

The runner now accepts focused Playwright file/options. Its default path uses separate fresh stacks for existing first-run/active-Case files and the new workflow file. Existing development/gateway, host, and viewport configuration remains available; only gateway/127.0.0.1/desktop was run for this effort. Disposable stack containers and volumes were removed and absence checked. Logs and copied artifacts were retained locally under `/tmp/coverage-browser-run.log` and `/tmp/coverage-browser-artifacts` for this implementation session; rerunning produces a fresh selected artifact directory.

The browser-only checks are not included in the V8 unit/component percentages. Their real backend contracts complement the controlled-HTTP suites; they do not establish unrun topologies, viewports, template backend journeys, or comprehensive frontend behavior.
